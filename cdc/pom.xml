<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.iris</groupId>
        <artifactId>iris4j</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>iris4j-cdc</artifactId>
    <name>iris4j-cdc</name>
    <description>Stream 消费、事件转换、幂等投影（不负责对外查询）</description>

    <dependencies>
        <dependency>
            <groupId>com.iris</groupId>
            <artifactId>iris4j-application</artifactId>
        </dependency>
        <dependency>
            <groupId>com.iris</groupId>
            <artifactId>iris4j-infrastructure</artifactId>
        </dependency>
        <!-- 直接使用 shared 的 IrisMetrics 门面，不靠传递依赖 -->
        <dependency>
            <groupId>com.iris</groupId>
            <artifactId>iris4j-shared</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
        </dependency>
    </dependencies>
</project>
